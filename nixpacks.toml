# Railway / Nixpacks build configuration for the Next.js app.
# Railway sets $PORT at runtime; `next start` reads it via the start script.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
